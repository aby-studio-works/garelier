# Promote: {{date}}

<!--
  Path: __garelier/<pm_id>/control/reports/promote/<YYYY-MM-DD>.md
  Owner: PM
  Reviewer: User (this document is shown for explicit approval)

  Generated when the user requests a promote (studio → target merge).
  PM compiles this from the studio branch state, presents to user,
  waits for explicit approval, then dispatches Concierge to merge
  garelier/<target-slug>/<pm_id>/studio into <target>.

  "Promote" replaces the v1.x "release" term. This document also
  replaces the old release_candidates/<date>.md.
-->

## Summary

- Promote date: {{YYYY-MM-DD}}
- Source: `garelier/{{target_slug}}/studio` ({{commit_sha}})
- Target: `{{target}}` (currently at {{previous_target_sha}})
- Generated by: PM
- Status: {{draft | awaiting approval | approved | promoted | aborted}}

## Diff at a glance

- Commits in studio not in target: {{N}}
- Files changed: {{N}}
- Authors: {{worker_ids_or_roles}}

## Completed milestones in this window

<!-- Milestones that moved from "active" to "shipped" since last promote. -->

- ✓ **{{milestone_name}}** — {{shipped_date}}
  - {{one-line value summary}}

(Empty list is OK — sometimes a promote ships partial milestones.)

## Blueprints shipped (or partially shipped)

<!-- Each entry: blueprint slug + completion status + short description. -->

- ✓ `{{blueprint_slug}}` — {{description}}
- ⚙ `{{blueprint_slug}}` (partial — phases 1-2 of 3) — {{description}}

## Quality gate results

Run the project's quality gate before requesting user approval. Capture
the results here.

| Check                          | Status   | Output                  |
| ------------------------------ | -------- | ----------------------- |
| `cargo check --workspace`      | {{✓/✗}}  | {{summary or "green"}}  |
| `cargo test --workspace`       | {{✓/✗}}  | {{N tests passed}}      |
| `{{project-specific gate}}`    | {{✓/✗}}  | {{summary}}             |

If any gate is failing, this promote should not be approved as-is.

## Base-tracking status

<!-- Confirm that `git merge <target>` into the studio branch succeeded
     (or that any conflicts were resolved per
     garelier-pm/references/promote-and-agents.md §7.5) before computing
     the promote diff. -->

- Last base-tracking merge: {{ISO8601_timestamp}} — {{clean | resolved-conflicts}}
- Resolution notes: {{(if conflicts) brief description | "(none)"}}

## Promote checklist (from operations/promote_checklist.md)

- [ ] Studio branch is clean (no uncommitted changes).
- [ ] All workbench branches are merged or explicitly abandoned.
- [ ] Required tests passed.
- [ ] Quality gates in `control/project_dashboard/quality_gates.md` are satisfied.
- [ ] Active risks in `control/project_dashboard/risks.md` are reviewed.
- [ ] Runtime manifest is consistent with reality.
- [ ] Smith hardening targets remaining is 0, or PM recorded an explicit user waiver.
- [ ] No production data write is pending (every data-changing blueprint per `data_change_policy.md` has run dry-run AND has explicit user approval on file).
- [ ] User explicitly approved this promote.

## Known issues being promoted

<!-- Be honest. Issues that exist on the studio branch and will land
     in <target>. The user should knowingly accept these. -->

- {{issue_1}}: {{severity}} — {{workaround_if_any}}
- (none)

## Promote notes (draft)

<!-- User-facing summary. Edit until the user approves the wording. -->

### What's new

- {{user-facing change 1}}
- {{user-facing change 2}}

### Improvements

- {{improvement_1}}

### Fixed

- {{fix_1}}

### Known limitations

- {{limitation_1}}

## Approval

- [ ] User has reviewed this document
- [ ] User has explicitly approved the merge
- [ ] Promote notes are finalized
- [ ] Tag name (if any): `v{{version}}`

## Post-promote actions

The `studio`→`{{target}}` merge, tag, and push are performed by **Concierge**
(DEC-025 / DEC-045; it returns `target_before_sha` / `target_after_sha`). After the promote lands,
PM:

1. Record the `target_after_sha` from the Concierge report.
2. Keep this document at `__garelier/<pm_id>/control/reports/promote/<YYYY-MM-DD>.md` as the persistent record.
3. Update `__garelier/<pm_id>/control/project_dashboard/roadmap.md`: mark shipped milestones, move them to "Recently promoted".
4. Move shipped blueprints from `__garelier/<pm_id>/control/blueprints/<slug>.md` to `__garelier/<pm_id>/control/blueprints/archive/<slug>.md`.
5. Append a `promoted` entry to `__garelier/<pm_id>/_pm/history.md`.
6. Notify Dock (write to `__garelier/<pm_id>/runtime/dock/inbox/`)
   so Dock can reflect the promote in `runtime/manifest.md`.

## Audit trail

- Generated at: {{ISO8601_timestamp}}
- Approved at: {{ISO8601_timestamp or "-"}}
- Promoted at: {{ISO8601_timestamp or "-"}}
- Final target SHA: {{commit_sha or "-"}}
