# Project Dashboard

Tracked mixed authority for schema 3.

- `current.md` and `notes.md` are curated durable memory.
- `roadmap.md` and `backlog.md` contain curated direction/view text plus
  generated indexes only inside explicit markers.
- Marker-external text is never generated or reformatted.
- Detail lives in canonical per-record Markdown files.
