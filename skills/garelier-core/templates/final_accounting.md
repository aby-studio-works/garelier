# Dock Final Accounting

This artifact is generated after proxy admission, mandatory scanning, and the project gate.
Gate roles consume it together with the producer report; the producer is not resumed to restate Dock-owned facts.

- Branch: `{{branch}}`
- Declared base SHA: `{{base_sha}}`
- Proxy / review SHA: `{{review_sha}}`
- Canonical producer result: `{{producer_result}}`
- Guardian scan: `{{guardian_scan}}`
- Mandatory scanner evidence: `{{scanner_evidence}}`
- Mandatory scanner evidence JSON: `{{scanner_evidence_json}}`
- Gate log: `{{gate_log}}`
- Driver-owned [gate] fields overwritten: {{driver_owned_overwrites}}
- Gate script source: {{gate_script_source}}
- Gate run source: {{gate_run_source}}
- Gate result: {{gate_result}}
- Coverage: {{coverage}}
{{coverage_details}}
- Coverage map source: {{coverage_map_source}}
- Coverage map vs studio: {{coverage_map_vs_studio}}
- Gate summary: {{gate_summary}}
