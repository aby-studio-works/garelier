# Garelier Control

Persistent project authority for one Garelier control namespace.

Read `control.toml`, then `project_dashboard/README.md`. Use the canonical
formats in `templates/`; do not invent artifact layouts. The sibling
`runtime/` tree is transient and must remain gitignored.
