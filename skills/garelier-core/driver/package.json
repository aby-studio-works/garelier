{
  "name": "garelier-driver",
  "version": "1.0.0",
  "description": "Garelier coordination tooling: merge gate, Status Web console, config, graphs (dispatch-only; the headless driver was deleted, DEC-066). TypeScript + Bun; no SDK dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "status": "bun run src/status_web.ts",
    "vendor:mermaid": "bun run scripts/vendor-mermaid.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
